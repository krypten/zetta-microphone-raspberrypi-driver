module.exports = function(server) {
  var events = ['_peer/connect', '_peer/disconnect'];
  events.forEach(function(event) {
    server.pubsub.subscribe(event, function(event, info) {
      console.log(info.peer.ws.upgradeReq.headers)
    });
  });
};
